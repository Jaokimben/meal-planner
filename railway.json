{
  "build": {
    "builder": "nixpacks",
    "config": {
      "startCmd": "bun install && expo start --tunnel"
    }
  },
  "deploy": {
    "startCommand": "bun install && expo start --tunnel"
  }
}